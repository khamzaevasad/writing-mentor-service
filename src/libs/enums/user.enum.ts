export enum UserType {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum UserStatus {
  ACTIVE = "ACTIVE",
  BLOCK = "BLOCK",
  DELETE = "DELETE",
}

export enum TargetLevel {
  THREE = 3,
  FOUR = 4,
  FIVE = 5,
  SIX = 6,
}
